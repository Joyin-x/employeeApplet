<view class="page">
    <view class="page__hd">

    </view>
    <view class="page__bd">
        <view class="weui-grids">
            <block wx:for="{{grids}}" wx:key="*this">
                <navigator url="/pages/list/list" class="weui-grid" hover-class="weui-grid_active">
                    <image class="weui-grid__icon" src="{{images[index]}}" />
                    <view class="weui-grid__label">{{item}}</view>
                </navigator>
            </block>
        </view>
    </view>
</view>

<view>
	<button type="primary" bindtap="query">点击签到</button>
</view>